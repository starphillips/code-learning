Pseudo:

I want to take the integer, read it, flip the numbers from back to front, read it again, then check if the integer is the same.
I would use an if statement to check if they’re the same
Y = x[::-1]
If x == y {
	Return isPalindrome = true }


``` ts
function isPalindrome(x: number): boolean {
    let y = x.toString();
    let z = y.split("").reverse().join("");
    if (y == z) {
        return true;
    }
    else return false;
};
```

